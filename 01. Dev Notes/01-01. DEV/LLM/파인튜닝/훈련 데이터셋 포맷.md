
#### Chat Template 포맷

**system, user, assistant** 구조는 대화형 LLM이나 인공지능 어시스턴트를 학습시킬 때 사용하는 **채팅 템플릿(Chat Template) 기반의 표준 메시지 포맷**입니다. OpenAI의 GPT 시리즈나 최신 오픈소스 LLM(Llama 3, Qwen 등)이 대화를 이해하고 응답하는 방식을 그대로 모사한 구조입니다.

**1. 각 역할(Role)별 정의 및 기능**
- **system (시스템 프롬프트):** 모델의 **페르소나, 규칙, 제약 사항, 혹은 출력 형식을 지정**하는 최상위 지시문입니다. 대화 전체에 지속적인 영향을 미칩니다.
	- _예시:_ `"너는 10년 차 수석 백엔드 개발자야. 답변은 항상 마크다운 형식으로 간결하게 작성해."`

- **user (사용자 입력):** 모델에게 던지는 **질문, 요청, 혹은 프롬프트**입니다.
    - _예시:_ `"Webpack 설정에서 소스맵을 활성화하는 방법을 알려줘."`

- **assistant (어시스턴트 응답):** 사용자의 요구에 대해 모델이 출력해야 할 정답(Ground Truth)입니다.
    - _예시:_ `"webpack.config.js 파일에서 devtool 옵션을 'source-map'으로 설정하면 됩니다..."`

**2. JSON/JSONL 데이터셋 포맷 예시**

이 구조는 주로 ShareGPT 포맷이나 최신 허깅페이스 대화형 데이터셋 표준(Conversational Schema)으로 저장됩니다.

```
[
  {
    "messages": [
      {
        "role": "system",
        "content": "너는 숙련된 TypeScript 및 Node.js 엔지니어다."
      },
      {
        "role": "user",
        "content": "Express 서버에서 JSON 바디 파싱 미들웨어 설정하는 법을 알려줘."
      },
      {
        "role": "assistant",
        "content": "Express 4.16.0 버전 이후부터는 express.json() 빌트인 미들웨어를 사용합니다.\n\n```javascript\nconst express = require('express');\nconst app = express();\n\napp.use(express.json());\n```"
      }
    ]
  }
]
```

**3. 파인튜닝 시 이 구조를 사용하는 이유**
- **역할 분리 학습:** 모델이 단순히 텍스트를 이어 쓰는 것을 넘어, "지시(System/User)"와 "결과물(Assistant)"의 경계를 명확히 인식하여 환각을 줄이고 지시사항 준수 능력을 극대화할 수 있습니다.
 
- **멀티턴(Multi-turn) 확장 용이:** 대화가 길어질 경우 `[user -> assistant -> user -> assistant]` 형태로 배열을 확장하여 문맥을 유지하는 대화형 모델을 효과적으로 훈련할 수 있습니다.
 
- **토크나이저 호환성:** 허깅페이스의 `tokenizer.apply_chat_template()` 기능을 사용할 때 이 구조가 표준으로 쓰이므로, 훈련과 추론 시 프롬프트 형식을 일관되게 유지할 수 있습니다.

---

### Alpaca 포맷과 Chat Template 포맷 비교
```
Alpaca 포맷 (가장 범용적): 지시사항(`instruction`), 입력맥락(`input`, 선택사항), 정답(`output`) 구조

[
  {
    "instruction": "다음 코드를 TypeScript로 변환해 주세요.",
    "input": "function add(a, b) { return a + b; }",
    "output": "function add(a: number, b: number): number {\n  return a + b;\n}"
  }
]
```

**Alpaca 포맷**과 **채팅 템플릿(Chat Template) 기반 포맷**은 목적, 대화 구조, 그리고 최신 LLM 트렌드에서 다음과 같은 차이를 가집니다.

**1. 데이터 구조 및 필드 체계**

- **Alpaca 포맷:** 단일 턴(Single-turn) 지시-응답에 최적화된 평면적(Flat) 딕셔너리 구조입니다. `instruction`(지시), `input`(선택적 맥락), `output`(정답) 필드로 명확히 나뉩니다.

- **Chat Template 포맷:** 멀티턴(Multi-turn) 대화를 지원하는 중첩 리스트 구조입니다. `messages` 키 아래에 `role`(system, user, assistant)과 `content` 쌍이 배열 형태로 순차적으로 쌓입니다.

**2. 시스템 프롬프트(System Prompt) 처리 방식**

- **Alpaca 포맷:** 시스템 프롬프트를 고정된 필드로 분리하기 애매해, 대개 `instruction` 내부에 문장 형태로 녹여내거나 별도의 체계가 없습니다.

- **Chat Template 포맷:** 최상단에 독립적인 `system` 역할을 두어 모델의 페르소나, 규칙, 제약 사항을 명확하고 일관되게 주입할 수 있습니다.

**3. 모델 아키텍처 및 학습 호환성**

- **Alpaca 포맷:** 과거 LLaMA 1~2 초기 시절이나 단순 명령어 수행(Instruction Following) 모델을 가볍게 학습할 때 주로 쓰였습니다.

- **Chat Template 포맷:** Llama 3, Qwen, Mistral 등 최신 오픈소스 LLM들이 기본 탑재한 채팅 템플릿(Chat Template)과 완벽히 호환됩니다. 허깅페이스의 `tokenizer.apply_chat_template()`을 거치면 모델이 이해하는 특수 토큰(`<|im_start|>`, `<|begin_of_text|>` 등)으로 자동 변환됩니다.

**비교 요약 표**

|**구분**|**Alpaca 포맷**|**Chat Template 포맷**|
|---|---|---|
|**구조 형태**|키-값 형태의 단일 객체 (`instruction`, `input`, `output`)|역할별 메시지 배열 (`messages` -> `role`, `content`)|
|**대화 확장성**|단일 질의응답(Single-turn)에 적합|연속적인 멀티턴(Multi-turn) 대화에 최적화|
|**시스템 제어**|명시적 역할 분리가 어려움|`system` 역할을 통해 페르소나 통제가 정밀함|
|**추세**|레거시 방식|현대 LLM 파인튜닝의 표준|