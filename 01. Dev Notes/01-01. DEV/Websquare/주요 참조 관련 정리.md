
반환타입
- 브라우저 window: 하위에 $p 존재
- WebSqaure Component
- Scope: 하위에 $p, scwin이 flat하게 존재
- Wframe, PageFrame : WebSqaure Component인 Wframe 그 자체
- Wframe, PageFrame : WebSqaure Component인 Wframe 그 자

----

config.xml의 w-pack rebuild시 자동 $p 붙임 여부 설정

---

$p.top()
$p.main()
$p.parent(): 부모 frame의 Scope
$p.getFrame(): 반환 타입 wframe, pageFrame
$p.getParentWindow(): 반환 타입 scope
$p.scwin

---
브라우저 팝업
popupWindow = $p.getPopupWindow(`_KB_POPUP_`): return이 브라우저 window

레이어(프레임팝업)
popupWindow = $p.getPopupWindow(`_KB_POPUP_`): return이 팝업의 scope

$p.getAllPopupList(): 배열 요소 내의 popupWin 속성 === $p.getPopupWindow() 

popupWindow.$p.top() === $p.getPopupWindow(`_KB_POPUP_`) --> 로드 --> pageContainer_WEBTOP.xml(X) --> popupPageContainer_WEBTOP.xml

popupWindow.$p.parent() === 호출한 부모가 아니라 index.xml

---

wsComponent.getScopeWindow() : 반환 타입 wframe, pageFrame
현재 컴포넌트의 부모 wframe의 부모 wframe을 반환하고 없으면 window 전역객체 반환

wsComponent.getScope()
현재 컴포넌트의 부모 wframe 컴포넌트를 반환

---

$p.getFrame().getObj("scwin") / $p.getFrame().getObj("ibx_aaa")
$p.getFrame().getWindow() === $p.getFrame().scope

wframe.getWindow().scwin === wframe.getObj("scwin")
wframe.getWindow() === wframe.scope

---

tabControl.getFrame(0).getWindow()
첫번째 탭의 scope 접근

tabControl.getWindow("tabId")
tabId에 해당하는 scope에 접근

tabControl.getWindow()
현재 선택된 tab의 scope에 접근

---


