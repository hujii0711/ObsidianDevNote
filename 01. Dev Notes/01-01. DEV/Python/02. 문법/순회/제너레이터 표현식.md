
## 제너레이터 표현식 (Generator Expression)

제너레이터 표현식은 **리스트 컴프리헨션과 문법이 거의 같지만, 대괄호 `[]` 대신 소괄호 `()`를 사용**하여 만드는 표현식입니다. 결과를 한 번에 리스트로 만들지 않고, **값을 하나씩 필요할 때마다 생성(지연 평가, lazy evaluation)**하는 제너레이터 객체를 반환합니다.

## 기본 문법

```python
(표현식 for 변수 in 반복가능객체 if 조건)
```

```python
gen = (x**2 for x in range(5))
print(gen)  # <generator object <genexpr> at 0x...>

for value in gen:
    print(value)
# 0 1 4 9 16
```

## 리스트 컴프리헨션과의 비교

```python
# 리스트 컴프리헨션 - 즉시 모든 값을 메모리에 저장
list_comp = [x**2 for x in range(5)]
print(list_comp)  # [0, 1, 4, 9, 16]

# 제너레이터 표현식 - 값을 즉시 만들지 않고 필요할 때 하나씩 생성
gen_exp = (x**2 for x in range(5))
print(gen_exp)  # <generator object ...>
print(list(gen_exp))  # [0, 1, 4, 9, 16] (직접 순회해야 값 확인 가능)
```

|구분|리스트 컴프리헨션 `[]`|제너레이터 표현식 `()`|
|---|---|---|
|반환값|리스트 (전체 값 저장)|제너레이터 객체|
|메모리|모든 요소를 한 번에 저장|값을 하나씩 생성 (메모리 절약)|
|평가 시점|즉시 평가(eager)|지연 평가(lazy)|
|재사용|여러 번 순회 가능|한 번 순회하면 소진됨|

## 특징 1: 메모리 효율성

큰 데이터를 다룰 때 리스트 컴프리헨션은 모든 값을 메모리에 올리지만, 제너레이터는 값을 하나씩만 만들어서 메모리를 훨씬 적게 사용합니다.

```python
import sys

list_comp = [x for x in range(1000000)]
gen_exp = (x for x in range(1000000))

print(sys.getsizeof(list_comp))  # 매우 큼 (수 MB)
print(sys.getsizeof(gen_exp))    # 매우 작음 (약 200바이트 정도)
```

## 특징 2: 한 번 소진되면 재사용 불가

제너레이터는 이터레이터(iterator)이기 때문에, 한 번 순회하고 나면 다시 처음부터 순회할 수 없습니다.

```python
gen = (x for x in range(3))

print(list(gen))  # [0, 1, 2]
print(list(gen))  # [] -> 이미 소진됨!
```

## 특징 3: `next()`로 값을 하나씩 꺼내기

```python
gen = (x**2 for x in range(3))

print(next(gen))  # 0
print(next(gen))  # 1
print(next(gen))  # 4
print(next(gen))  # StopIteration 에러 (더 이상 값 없음)
```

## 특징 4: 함수 인자로 바로 사용할 때 괄호 생략 가능

제너레이터 표현식이 함수의 **유일한 인자**로 쓰일 경우, 소괄호를 한 번만 써도 됩니다.

```python
# 괄호 생략 가능 (함수 호출 괄호와 겸용)
total = sum(x**2 for x in range(5))
print(total)  # 30

# 다른 인자가 있으면 반드시 괄호로 감싸야 함
result = sorted((x for x in [3, 1, 2]), reverse=True)
```

## 조건문 활용

리스트 컴프리헨션처럼 `if` 조건을 붙일 수 있습니다.

```python
gen = (x for x in range(10) if x % 2 == 0)
print(list(gen))  # [0, 2, 4, 6, 8]
```

## 언제 사용하나?

|상황|추천|
|---|---|
|결과를 여러 번 순회해야 함|리스트 컴프리헨션|
|결과를 인덱싱해야 함 (`result[3]`)|리스트 컴프리헨션|
|데이터가 매우 크거나 무한할 수 있음|제너레이터 표현식|
|한 번만 순회하며 값을 소비 (예: `sum()`, `for`문)|제너레이터 표현식|
|메모리 효율이 중요함|제너레이터 표현식|

## 실전 예제: 큰 파일 처리

```python
# 파일이 매우 커도 한 줄씩만 메모리에 올리므로 효율적
with open("huge_file.txt") as f:
    long_lines = (line for line in f if len(line) > 100)
    for line in long_lines:
        print(line)
```

이처럼 제너레이터 표현식은 **"모든 값을 미리 만들 필요 없이, 필요할 때마다 하나씩 계산해서 쓴다"**는 개념이 핵심입니다. 대용량 데이터나 무한 시퀀스를 다룰 때 특히 유용합니다.

더 나아가서 `yield`를 사용하는 **제너레이터 함수**와의 차이점도 궁금하시면 말씀해주세요!