# .bat 파일을 아이콘 있는 실행파일로 배포하는 방법

## 방법 비교 요약

| 방법 | 난이도 | 아이콘 | 단일 파일 | 무료 |
|------|--------|--------|-----------|------|
| 바로가기(.lnk) | ⭐ | ✅ | ❌ | ✅ |
| Bat To Exe Converter | ⭐⭐ | ✅ | ✅ | ✅ |
| PS2EXE (PowerShell) | ⭐⭐ | ✅ | ✅ | ✅ |
| Inno Setup (설치파일) | ⭐⭐⭐ | ✅ | ✅ | ✅ |
| NSIS | ⭐⭐⭐ | ✅ | ✅ | ✅ |

---

## 방법 1 — 바로가기 (.lnk) 만들기 [가장 간단]

bat 파일은 유지하고 아이콘만 바꾼 바로가기 배포

```
1. bat 파일 우클릭 → "바로 가기 만들기"
2. 생성된 .lnk 파일 우클릭 → 속성
3. "아이콘 변경" 클릭
4. .ico 파일 경로 지정
5. 확인
```

**단점:** bat 파일과 lnk 파일을 **같이 배포**해야 함

---

## 방법 2 — Bat To Exe Converter [추천 ⭐]

bat을 진짜 .exe로 변환, 아이콘 포함 단일 파일 생성

### 설치 및 사용
```
1. https://bat-to-exe-converter.en.softonic.com 에서 다운로드
2. 실행 후 bat 파일 불러오기
3. 아이콘(.ico) 파일 지정
4. 옵션 설정:
   - Invisible: 콘솔창 숨김 여부
   - x64: 64비트 빌드
   - UAC: 관리자 권한 요청
5. Compile 클릭 → .exe 생성
```

### 주요 옵션
```
☑ Invisible Application  → 콘솔 창 안 보임 (백그라운드 실행)
☑ Overwrite Temp Files   → 임시파일 덮어쓰기
☑ Request Admin Rights   → 관리자 권한 자동 요청 (UAC)
```

---

## 방법 3 — PS2EXE (무료, 커맨드라인)

PowerShell 기반으로 bat → exe 변환

```powershell
# PowerShell 관리자 권한으로 실행

# 1. 모듈 설치
Install-Module ps2exe -Scope CurrentUser

# 2. bat을 ps1으로 먼저 래핑
# run.ps1 내용:
# Start-Process "cmd.exe" -ArgumentList "/c myscript.bat" -Wait

# 3. exe로 변환
Invoke-ps2exe `
  -inputFile "run.ps1" `
  -outputFile "MyApp.exe" `
  -iconFile "myicon.ico" `
  -title "내 앱" `
  -version "1.0.0" `
  -noConsole        # 콘솔 창 숨김
```

---

## 방법 4 — Inno Setup (설치 프로그램으로 배포) [전문 배포]

설치 마법사 형태로 배포, 가장 전문적인 방법

### 설치
```
https://jrsoftware.org/isinfo.php 에서 무료 다운로드
```

### 스크립트 예시
```ini
[Setup]
AppName=내 애플리케이션
AppVersion=1.0
DefaultDirName={autopf}\MyApp
DefaultGroupName=MyApp
OutputBaseFilename=MyApp_Setup
SetupIconFile=myicon.ico       ; 설치파일 아이콘
UninstallDisplayIcon={app}\myicon.ico

[Files]
; bat 파일과 아이콘 포함
Source: "myscript.bat"; DestDir: "{app}"
Source: "myicon.ico";   DestDir: "{app}"

[Icons]
; 바탕화면 + 시작메뉴 아이콘 생성
Name: "{autodesktop}\내 앱";    Filename: "{app}\myscript.bat"; IconFilename: "{app}\myicon.ico"
Name: "{autostartmenu}\내 앱";  Filename: "{app}\myscript.bat"; IconFilename: "{app}\myicon.ico"

[Run]
; 설치 후 바로 실행 옵션
Filename: "{app}\myscript.bat"; Description: "지금 실행"; Flags: postinstall
```

→ **단일 Setup.exe** 생성, 더블클릭으로 설치

---

## 아이콘(.ico) 파일 만들기

### 온라인 변환 (PNG → ICO)
```
https://convertio.co/png-ico/
https://icoconvert.com
```

### PowerShell로 직접 생성
```powershell
# png를 ico로 변환
Add-Type -AssemblyName System.Drawing
$img = [System.Drawing.Image]::FromFile("C:\icon.png")
$icon = [System.Drawing.Icon]::FromHandle(
  ([System.Drawing.Bitmap]$img).GetHicon()
)
$stream = [System.IO.FileStream]::new("C:\icon.ico", "Create")
$icon.Save($stream)
$stream.Close()
```

---

## 배포 시나리오별 추천

```
👤 개인/소규모 사용     → 방법 2 (Bat To Exe Converter)
🏢 사내 배포            → 방법 4 (Inno Setup, 설치/제거 지원)
⚡ 빠른 테스트          → 방법 1 (바로가기)
🔧 자동화 빌드 파이프라인 → 방법 3 (PS2EXE, CLI 기반)
```

**bat 내용이 단순하다면 방법 2 (Bat To Exe Converter)가 가장 빠르고 쉽습니다.** 어떤 시나리오인지 알려주시면 더 구체적으로 도와드릴게요! 😊