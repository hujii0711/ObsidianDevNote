---
year: 2026
month: 4
---


```dataviewjs
const fm = dv.current().file.frontmatter || {};
const fname = dv.current().file.name;
const fnMatch = fname.match(/(\d{4})[-년\s]*\s*(\d{1,2})/);
const now = new Date();
const YEAR  = Number(fm.year)  || (fnMatch && Number(fnMatch[1])) || now.getFullYear();
const MONTH = Number(fm.month) || (fnMatch && Number(fnMatch[2])) || (now.getMonth() + 1);

const isDark = document.body.classList.contains("theme-dark");
const c = isDark
  ? { border:"#30363d", bg:"#0d1117", headerBg:"#161b22", text:"#e6edf3", muted:"#7d8590", danger:"#f85149", dangerBg:"#21262d" }
  : { border:"#d0d7de", bg:"#ffffff", headerBg:"#f6f8fa", text:"#1f2328", muted:"#656d76", danger:"#cf222e", dangerBg:"#f6f8fa" };

const wrap = dv.el("div", "", { attr: {
  style: `display:flex; align-items:center; justify-content:space-between; gap:12px; padding:14px 16px; border:1px solid ${c.border}; border-radius:6px; background:${c.headerBg}; font-family:-apple-system,BlinkMacSystemFont,'Segoe UI',Helvetica,Arial,sans-serif; margin:8px 0;`
}});
wrap.innerHTML = `<div style="font-size:18px; font-weight:600; color:${c.text};">📋 ${YEAR}년 ${MONTH}월 Todo List</div>`;

const btn = document.createElement("button");
btn.textContent = "🗑 초기화";
btn.setAttribute("style", `padding:6px 12px; font-size:13px; font-weight:500; color:${c.danger}; background:${c.dangerBg}; border:1px solid ${c.border}; border-radius:6px; cursor:pointer;`);
btn.onclick = async () => {
  const ok = confirm(`${YEAR}년 ${MONTH}월의 모든 할일을 삭제하시겠습니까?\n(완료/미완료 모두 제거됩니다)`);
  if (!ok) return;
  const file = app.vault.getAbstractFileByPath(dv.current().file.path);
  const content = await app.vault.read(file);
  const cleared = content.replace(/^>\s*- \[[ xX]\].*$\n?/gm, "");
  await app.vault.modify(file, cleared);
  new Notice("할일 목록이 초기화되었습니다.");
};
wrap.appendChild(btn);
```

> [!note] 할일 목록
> - [x] 맥에서 vscode 비활성화해도 안죽음 터미널 경고와의 연관성 확인
> - [ ] 맥 빌드 starup.sh시 오류 있음
> - [x] 맥에서 클론후 바로 빌드시 npm 오류 발생 셋업 진행이 필수 인지 확인
> - [x] Eslint검사후 보고서
> - [x] Eslint for문에서 최적화
> - [x] 변수선언후 할당 안하면 error
> - [x] 옵시디언 데스크탑에서도 md 입력바 있는지 확인
> - [x] eslint error의 경우민 커밋 안되나?
> - [ ] vscode개발환경에서 xml 편집하고 wpack 돌릴수 있게 작업환경 개선 (sms 랜덤포트 선행) workspace 유동적 지정
> - [ ] map 생성시 좋은점 확인
> - [x] git tracked untracked commited uncommited staged 확실히 구분 working tree
> - [ ] ws.codeworkspace untracked하게
> - [x] NODE_ENV production 안먹음
> - [x] aiagent 시작과 중지 로직 확인 --> treedb 정리
> - [x] package.json, start 모듈
> - [x] 분기 os가 맥일때 server.pid로 kill 
> - [ ] devkit 서버 버전 신규 라우터 전달
> - [ ] ext env.profile 추가
> - [x] 파일 서비스 os  분기
> - [x] sms pid 실행증이면 죽이고 시작하기
> - [ ] 이미 띄워진 xml 탭에도 webviewTarget add
> - [ ] ai agent 로딩 지연 막기
> - [ ] sms 랜덤 포트
> - [x] 배포시 불필요한 폴더 체크
> - [ ] .git/index.lock 파일 확인
> - [x] jetty가 떠있는 상태 확인해도 바로 안그려짐
> - [x] vsix 패키징 제외 .vscodeignore에 지정
> - [ ] studioAPI.js push xml 비활성화시 안내 메시지 출력
> - [ ] 옵시디언의 속성, 태그, 백링크, 나가는 링크에 대해서
> - [ ] svn trunk가 붙는지 안붙는지 차이

## 진행 현황

```dataviewjs
const tasks = dv.current().file.tasks;
const todo = tasks.where(t => !t.completed);
const done = tasks.where(t => t.completed);

const isDark = document.body.classList.contains("theme-dark");
const c = isDark ? {
  border: "#30363d", bg: "#0d1117", headerBg: "#161b22",
  text: "#e6edf3", muted: "#7d8590", rowBorder: "#21262d",
  badgeBg: "#30363d", badgeText: "#e6edf3",
  todo: "#d29922", done: "#3fb950"
} : {
  border: "#d0d7de", bg: "#ffffff", headerBg: "#f6f8fa",
  text: "#1f2328", muted: "#656d76", rowBorder: "#eaeef2",
  badgeBg: "#eaeef2", badgeText: "#1f2328",
  todo: "#bf8700", done: "#1a7f37"
};

const container = dv.el("div", "", { attr: {
  style: `border:1px solid ${c.border}; border-radius:6px; background:${c.bg}; font-family:-apple-system,BlinkMacSystemFont,'Segoe UI',Helvetica,Arial,sans-serif; overflow:hidden; margin:8px 0;`
}});

const total = tasks.length;
const pct = total === 0 ? 0 : Math.round((done.length / total) * 100);
const summary = document.createElement("div");
summary.setAttribute("style", `padding:12px 16px; background:${c.headerBg}; border-bottom:1px solid ${c.border}; display:flex; align-items:center; gap:12px; flex-wrap:wrap; font-size:13px; color:${c.text};`);
summary.innerHTML = `
  <span style="font-weight:600;">전체 ${total}건</span>
  <span style="display:inline-flex;align-items:center;gap:6px;"><span style="width:8px;height:8px;border-radius:50%;background:${c.todo};"></span>미완료 <b>${todo.length}</b></span>
  <span style="display:inline-flex;align-items:center;gap:6px;"><span style="width:8px;height:8px;border-radius:50%;background:${c.done};"></span>완료 <b>${done.length}</b></span>
  <span style="flex:1; min-width:120px; height:8px; background:${c.rowBorder}; border-radius:999px; overflow:hidden;">
    <span style="display:block; height:100%; width:${pct}%; background:${c.done};"></span>
  </span>
  <span style="color:${c.muted};">${pct}%</span>
`;
container.appendChild(summary);

function section(title, count, items, color) {
  const header = document.createElement("div");
  header.setAttribute("style", `padding:10px 16px; background:${c.headerBg}; border-bottom:1px solid ${c.border}; font-size:14px; font-weight:600; color:${c.text}; display:flex; align-items:center; gap:8px;`);
  header.innerHTML = `<span style="display:inline-block;width:10px;height:10px;border-radius:50%;background:${color};"></span>${title} <span style="background:${c.badgeBg};color:${c.badgeText};border-radius:20px;padding:0 8px;font-size:12px;font-weight:500;">${count}</span>`;
  container.appendChild(header);

  const body = document.createElement("div");
  body.setAttribute("style", `padding:8px 16px; background:${c.bg};`);
  if (items.length === 0) {
    body.innerHTML = `<div style="color:${c.muted};font-size:13px;padding:8px 0;">항목 없음</div>`;
  } else {
    items.forEach((t, i) => {
      const row = document.createElement("div");
      row.setAttribute("style", `padding:6px 0; border-bottom:1px solid ${c.rowBorder}; font-size:13px; color:${c.text}; display:flex; align-items:flex-start; gap:8px;`);
      const checked = t.completed ? "checked" : "";
      const textStyle = t.completed ? `color:${c.muted};text-decoration:line-through;` : "";
      row.innerHTML = `<span style="min-width:24px;text-align:right;color:${c.muted};font-variant-numeric:tabular-nums;">${i + 1}.</span><input type="checkbox" ${checked} disabled style="margin-top:3px;"><span style="${textStyle}">${t.text}</span>`;
      body.appendChild(row);
    });
    body.lastChild.style.borderBottom = "none";
  }
  container.appendChild(body);
}

section("⬜ 미완료", todo.length, todo, c.todo);

const divider = document.createElement("div");
divider.setAttribute("style", `height:1px;background:${c.border};`);
container.appendChild(divider);

section("✅ 완료", done.length, done, c.done);
```

