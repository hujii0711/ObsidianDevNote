# CI/CD 개념

## 한 줄 정의

```
CI = 코드를 자주 합치고 자동으로 검증
CD = 검증된 코드를 자동으로 배포
```

---

## CI (Continuous Integration, 지속적 통합)

### 탄생 배경

과거 개발 방식의 문제에서 시작했습니다.

```
개발자 A가 2주간 feature-A 브랜치 작업
개발자 B가 2주간 feature-B 브랜치 작업
    ↓
합치는 날 → 충돌 폭발, 버그 대량 발생
"Integration Hell" 발생
```

### CI가 해결하는 것

```
매일 또는 커밋마다 메인 브랜치에 통합
    ↓
자동으로 빌드 + 테스트 실행
    ↓
문제를 즉시 발견 → 작은 단위로 수정
```

### CI 파이프라인이 하는 일

```
코드 푸시
  → 코드 체크아웃
  → 의존성 설치
  → 빌드
  → 유닛 테스트
  → 통합 테스트
  → 코드 품질 검사 (Lint)
  → 보안 취약점 스캔
  → 결과 리포트
```

---

## CD (Continuous Delivery/Deployment)

CD는 두 가지 의미가 있습니다.

### Continuous Delivery (지속적 전달)

CI를 통과한 코드를 **배포 가능한 상태**로 만들어두는 것
배포 자체는 사람이 승인 후 실행

```
CI 통과
  → 스테이징 환경 자동 배포
  → QA 검증
  → 담당자 승인 버튼 클릭
  → 프로덕션 배포
```

### Continuous Deployment (지속적 배포)

CI를 통과하면 **자동으로 프로덕션까지** 배포

```
CI 통과
  → 프로덕션 자동 배포 (사람 개입 없음)
```

### 차이 요약

```
Continuous Delivery   → 배포 준비 자동화 + 사람이 최종 승인
Continuous Deployment → 배포까지 완전 자동화
```

---

## CI/CD 전체 흐름

```
[개발자]
  코드 작성 → git push
        ↓
[CI 단계]
  빌드 → 테스트 → 품질검사
        ↓ 통과
[CD 단계]
  스테이징 배포 → 검증
        ↓ 통과
  (승인) → 프로덕션 배포
        ↓
[운영]
  모니터링 → 이상 감지 → 롤백
```

---

## 핵심 개념들

### 파이프라인 (Pipeline)
CI/CD의 각 단계를 순서대로 연결한 흐름

```
Stage 1    Stage 2    Stage 3    Stage 4
빌드    →  테스트  →  패키징  →  배포
  ↓실패       ↓실패       ↓실패
 중단        중단        중단
```

### 아티팩트 (Artifact)
빌드 결과물. 다음 단계로 전달되는 산출물

```
소스코드 → [빌드] → JAR / Docker Image / ZIP
                         ↓
                    이것이 아티팩트
                    (테스트, 배포에 재사용)
```

### 환경 (Environment)
코드가 실행되는 단계별 서버

```
dev(개발) → staging(검증) → production(운영)
  개발자        QA팀           실제 사용자
```

### 롤백 (Rollback)

배포 후 문제 발생 시 이전 버전으로 되돌리기

```
v1.0 운영 중
  → v1.1 배포
  → 장애 발생
  → v1.0으로 자동 롤백
```

---

## CI/CD가 없을 때 vs 있을 때

| | CI/CD 없음 | CI/CD 있음 |
|--|-----------|-----------|
| 배포 주기 | 수주~수개월 | 하루 수십 번도 가능 |
| 버그 발견 | 배포 후 발견 | 커밋 즉시 발견 |
| 배포 방식 | 수동, 야근 | 자동, 주간에 |
| 롤백 | 수동, 위험 | 자동, 빠름 |
| 팀 협업 | 통합 지옥 | 지속적 통합 |

---

## 실무에서 자주 쓰는 전략

### Blue-Green 배포
```
현재: Blue(v1) 운영 중
새버전: Green(v2) 준비
    ↓
트래픽을 Green으로 전환
문제 시 Blue로 즉시 롤백
```

### Canary 배포
```
전체 트래픽 중 5%만 새버전으로
  → 문제 없으면 10% → 50% → 100%
  → 문제 발생 시 즉시 0%로 차단
```

### Feature Flag
```
코드는 배포했지만 기능은 OFF 상태
  → 준비되면 플래그만 ON으로 변경
  → 배포 없이 기능 활성화/비활성화
```

---

## 한 줄 요약

```
CI = "코드가 망가졌는지 즉시 알아내는 것"
CD = "망가지지 않았으면 자동으로 사용자에게 전달하는 것"
```

CI/CD는 도구가 아니라 **개발 문화이자 방법론**입니다.
GitHub Actions, Jenkins, GitLab CI는 이를 구현하는 도구일 뿐입니다.

특정 전략이나 도구 설정이 궁금하시면 말씀해 주세요! 😊