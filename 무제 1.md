# .command 파일에 아이콘 적용하기

## 결론부터

**.command 파일 자체에 아이콘을 직접 삽입하는 것은 불가능합니다.**
대신 아래 방법들로 아이콘처럼 보이게 할 수 있습니다.

---

## 방법 1 — Finder에서 아이콘 직접 변경 [가장 간단]

특정 파일 하나에만 아이콘 적용하는 방법입니다.

```
1. .icns 또는 .png 아이콘 파일 준비
2. 아이콘 파일을 미리보기(Preview)로 열기
3. cmd+A → cmd+C (이미지 복사)
4. .command 파일 우클릭 → 정보 가져오기 (cmd+I)
5. 정보 창 왼쪽 상단 아이콘 클릭 (파란 테두리 생김)
6. cmd+V 붙여넣기
```

**단점:** 배포 시 아이콘이 유지되지 않음, 매번 수동 적용 필요

---

## 방법 2 — .app 번들로 감싸기 [배포용 추천 ⭐]

.command를 .app으로 포장하면 아이콘 + 더블클릭 실행 모두 가능합니다.

### 폴더 구조 직접 만들기
```
MyApp.app/
└── Contents/
    ├── Info.plist
    ├── MacOS/
    │   └── launcher       ← 실행 스크립트
    └── Resources/
        └── myicon.icns    ← 아이콘
```

### 1. 실행 스크립트 작성
```bash
# Contents/MacOS/launcher 파일 내용
#!/bin/bash
cd "$(dirname "$0")"
open -a Terminal ./myscript.command
```

### 2. Info.plist 작성

```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN"
  "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <key>CFBundleName</key>
  <string>MyApp</string>
  <key>CFBundleExecutable</key>
  <string>launcher</string>
  <key>CFBundleIconFile</key>
  <string>myicon</string>          <!-- .icns 확장자 생략 -->
  <key>CFBundleIdentifier</key>
  <string>com.mycompany.myapp</string>
  <key>CFBundleVersion</key>
  <string>1.0</string>
</dict>
</plist>
```

### 3. 권한 부여
```bash
chmod +x MyApp.app/Contents/MacOS/launcher
```

→ Finder에서 **커스텀 아이콘으로 더블클릭 실행** 가능 ✅

---

## 방법 3 — Platypus 사용 [GUI 툴, 가장 편리 ⭐⭐]

.sh / .command → .app 변환 전용 무료 맥 앱

```
1. https://sveinbjorn.org/platypus 에서 다운로드
2. Script Path: myscript.command 선택
3. App Name: 앱 이름 입력
4. Icon: 커스텀 아이콘 .icns 지정
5. Interface: Terminal (터미널 창 표시)
   또는 None (백그라운드 실행)
6. Create 클릭 → .app 생성
```

**옵션 설정:**
```
☑ Remain running after execution  → 실행 후 창 유지
☑ Run with administrator privileges → 관리자 권한
☑ Accept dropped items            → 파일 드래그 앤 드롭 지원
```

---

## 아이콘(.icns) 파일 만들기

```bash
# PNG → icns 변환 (터미널)
mkdir MyIcon.iconset

# 필요한 사이즈 생성 (sips 명령어, macOS 기본 내장)
sips -z 16 16     icon.png --out MyIcon.iconset/icon_16x16.png
sips -z 32 32     icon.png --out MyIcon.iconset/icon_16x16@2x.png
sips -z 32 32     icon.png --out MyIcon.iconset/icon_32x32.png
sips -z 64 64     icon.png --out MyIcon.iconset/icon_32x32@2x.png
sips -z 128 128   icon.png --out MyIcon.iconset/icon_128x128.png
sips -z 256 256   icon.png --out MyIcon.iconset/icon_128x128@2x.png
sips -z 256 256   icon.png --out MyIcon.iconset/icon_256x256.png
sips -z 512 512   icon.png --out MyIcon.iconset/icon_256x256@2x.png
sips -z 512 512   icon.png --out MyIcon.iconset/icon_512x512.png
sips -z 1024 1024 icon.png --out MyIcon.iconset/icon_512x512@2x.png

# iconset → icns 변환
iconutil -c icns MyIcon.iconset
# → MyIcon.icns 생성됨
```

---

## 방법별 추천 시나리오

```
🧑‍💻 개발자 본인만 사용    → 방법 1 (Finder 아이콘 변경)
📦 일반 사용자 배포       → 방법 3 (Platypus, 가장 간편)
🏢 전문 앱 수준 배포      → 방법 2 (.app 번들 직접 제작)
```

배포 규모나 목적을 알려주시면 더 맞춤으로 안내드릴 수 있어요! 😊