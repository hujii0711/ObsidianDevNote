
config.xml의 w-pack rebuild시 자동 $p 붙임 여부 설정

---

$p.top()
$p.main()
$p.parent()
$p.getFrame()
$p.getPopupWindow()
$p.getAllPopupList()
$p.getParentWindow()
$p.scwin

---

component.getScopeWindow()
현재 컴포넌트의 부모 wframe의 부모 wframe을 반환하고 없으면 window 전역객체 반환
component.getScope()
현재 컴포넌트의 부모 wframe 컴포넌트를 반환
component.getObj()
component.getWindow()

---

$p.getFrame().getWindow().

---

tabControl.getFrame(0).getWindow()
첫번째 탭의 scope 접근

tabControl.getWindow("tabId")
tabId에 해당하는 scope에 접근

tabControl.getWindow()
현재 선택된 tab의 scope에 접근

---

wframe.getWindow().scwin === wframe.getObj("scwin")
wframe.getWindow() === wframe.scope
