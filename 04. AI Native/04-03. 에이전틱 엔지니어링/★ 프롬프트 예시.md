
>`"/prd.md 문서의 '3-1 회원가입' 요구사항과 /src/types/user.ts의 유저 타입을 참고해서, /src/components/SignupForm.tsx 컴포넌트를 구현해줘. 스타일링은 Tailwind CSS를 사용해야 해."`

>`"이제 터미널에서` npm run build`(또는`npm run lint`)를 실행해서 에러가 없는지 확인해줘. 만약 타입 에러나 컴파일 에러가 발생하면 에러 로그를 보고 코드를 스스로 수정해줘."`

>`"/prd.md의 요구사항과 비교했을 때 현재 코드에서 누락된 예외 처리나 비즈니스 로직이 있는지 전수 조사해줘. 그리고` .clauderules`에 명시한 Zustand 상태 관리 컨벤션을 깨뜨린 코드가 있다면 찾아서 리포트해줘."`

>`"보안 전문가 관점에서 현재 코드의 취약점을 분석해줘. 특히 SQL 인젝션, XSS(크로스 사이트 스크립팅), CSRF, 그리고 API Key나 비밀번호가 소스코드에 하드코딩(Hardcoded)된 부분이 없는지 샅샅이 찾아내고, 발견되면 안전한 코드로 리팩토링해줘."`

>`"현재 컴포넌트 구조에서 불필요한 리렌더링(Re-rendering)이 자주 발생할 만한 구간이나 메모리 누수(Memory Leak) 위험이 있는` useEffect `구문이 있는지 확인하고 최적화해줘."`

>`"DB 조회 로직 중에 N+1 Query 문제가 발생하여 성능을 저하시키는 구간이 없는지 보고, 인덱스(Index) 처리가 필요한 컬럼을 제안해줘."`

>`"여러 파일에 중복으로 작성된 유틸리티 로직이 있다면` utils/`폴더로 공통화해줘. 하나의 함수가 너무 많은 책임을 지고 있다면 잘게 쪼개주고, TypeScript에서 무분별하게 사용된`any `타입을 구체적인 인터페이스 타입으로 대체해줘."`

>지금부터 '코드 리뷰 및 보안/성능 검증' 단계를 시작한다. 
  내가 작성하라고 했던 기능들이 완성되었으니, 다음 3가지 관점에서 프로젝트 전체 코드를 검사하고 보고서를 작성해줘.
>
>1. 보안: 하드코딩된 Key, 인젝션 취약점, 데이터 검증 누락 여부
>2. 성능: 무분별한 루프, 불필요한 비동기 블로킹(Blocking), 메모리 누수 위험
>3. 품질: 중복 코드, 매직 넘버 사용, TypeScript 타입 안정성 
>
  이상이 발견된 파일과 라인 번호를 명시하고, 어떻게 고쳐야 하는지 리팩토링 코드 예시를 함께 제안해줘.

> AI 챗봇을 만들고 싶습니다. 명세를 함께 작성하고 싶으니 필요한 정보를 질문해줄 수 있나요?
> 최종적으로 CLAUDE.md 파일을 출력하는 것이 목적입니다.

>시작 초기 단계에서 Makefile에 개발 초기화부터 배포까지 필요한 명령을 다음과 같이 프롬프트로 지시하여 정리하는 것이다.
> `초기화 명령어, 개발 서버 실행, 빌드, 배포용 명령어를 Makefile에 정리하세요.`

>linter를 설치 및 설정하세요

>테스트 라이브러리를 설치 및 구성하세요.

>husky를 설치 및 설정하세요

>현재 명세와 구성에 따라 먼저 이슈를 자세하고 구체적으로 등록하세요. 그것을 gh 명령으로 깃허브에 배포하세요.

>Issue#1을 구현하세요. 브랜치를 생성해서 작업하고, 마지막에 풀 리퀘스트를 제출하세요.

>PR#1의 내용을 검토하세요. 풀 리퀘스트의 댓글로 리뷰 내용을 작성하세요.

>Issue `#3`구현하세요. git worktree을 사용해 하위 폴더를 만들고 별도 브랜치를 만든 후, 마지막으로 풀 리퀘스트를 수행하세요. 하위 폴더는 `issue-***` 이라는 명명 규칙을 따르세요.

>새로운 스킬을 작성하세요. 클로드 코드 에이전트가 파일 편집을 완료한 후, 편집된 파일에 대해 린터를 실행하는 스킬입니다.





