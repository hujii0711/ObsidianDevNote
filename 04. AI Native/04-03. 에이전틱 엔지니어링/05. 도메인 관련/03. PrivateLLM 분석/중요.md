
```
privateLLM/
├── pyproject.toml      # uv 워크스페이스 루트 (members: pipelines, packages/*, apps/*  /  exclude: apps/web)
├── uv.lock             # 단일 루트 락파일 (모든 Python 멤버 공유)
├── pipelines/          # ① 데이터 파이프라인 (법령·판례 수집 → 청크 → Chroma 인덱싱)
├── packages/           # ② 재사용 Python 라이브러리 (rag / eval / ftdata / finetune)
├── apps/               # ③ 실행 애플리케이션 (api 백엔드 + web 프론트)
├── data/               # ④ 모든 산출물·아티팩트 (gitignore — 재생성 가능)
├── docs/               # ⑤ 설계·계획·실험노트·export
└── .venv/              # 워크스페이스 공용 가상환경
```

```
[국가법령 API]                        pipelines (ingest→clean→chunk→index)
      │                                          │
      ▼                                          ▼
 data/raw  ──────────────────────────────►  data/chroma (jeonse_deposit)
                                                 │
                                  packages/rag (retriever+prompt+citations)
                                                 │
 apps/web ◄──SSE──  apps/api (/chat, run_chat) ──┴── llm.py (Qwen2.5-7B MLX)
                                                 │
                       packages/eval ◄───────────┤ (run_chat 재사용)
                       packages/ftdata ──► data/ft ──► packages/finetune ──► data/adapters/qlora
```


## 코드 의존 DAG (import만, 비순환)

```
pipelines        ← (누구도 import 안 함; 데이터로만 연결)
rag              ← api, eval, ftdata
api   → rag      ← eval, ftdata, finetune
eval  → rag, api ← ftdata, finetune
ftdata→ rag, api, eval
finetune→ api, eval
```

위상 정렬(아래→위로 의존):

|레이어|패키지|워크스페이스 의존|성격|
|---|---|---|---|
|L0|**pipelines**|(없음)|ETL·write, 완전 독립|
|L0|**rag**|(없음)|검색·프롬프트 코어, read|
|L1|**api**|rag|서빙(FastAPI/SSE)|
|L2|**eval**|rag, api|평가 하니스|
|L3|**ftdata**|rag, api, eval|학습데이터 빌더|
|L3|**finetune**|api, eval|QLoRA 학습·A/B|

→ **pipelines·rag가 두 뿌리**, api가 허브, eval이 그 위, ftdata·finetune이 최상위 소비자. 순환 없음.