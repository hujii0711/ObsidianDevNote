
#### 1. 파이썬 개발 환경
- 가상 환경(uv) 생성 및 의존성 설치
- vscode, Ruff(린트/포맷팅)
- NumPy, Pandas, PyTorch, TensorFlow, Keras, Scikit-learn

#### 2. 데이터 전처리
- RAG용 문서 전처리
- 파인튜닝용 Instruction/input/output 형식, train/valid/test 분할 완료
- AI Hub 법률 데이터셋 신청

#### 3. 파인튜닝
- 사전 발급 받아야할 API 키
- MLX(MLX 빌드에 필요한 도구: Xcode Command Line Tools)
- 허깅페이스에서 모델 다운로드
- 파운데이션 모델(Llama 3.1 8B Instruct, EEVE-Korean-10.8B)
- QLoRA = 4bit 양자화 모델 + LoRA 어댑터
- 하이퍼 파라미터 설정(config.yaml)
- 학습 완료후 LoRA 어댑터를 베이스 모델에 병합(mlx_lm.fuse)
- 모델 평가

#### 4. RAG
- 임베딩 모델(BGE-M3)
- 리랭킹 모델(jhgan/ko-sroberta-multitask)
- LangChain
--> 질의 --> 질의 임베딩 생성 --> 백터 유사도 검색 --> 리랭킹 --> 최종 문서 프롬프트에 삽입
- SQLite(대화 이력 저장)
- 프롬프트 엔지니어링
- 검색 평가, RAGAS (RAG 평가 프레임워크)

#### 5. 웹 서비스
- Next.js

#### 6. CI/CD 배포 전략
- docker
- GitHub Action CI 파이프라인
- E2E 테스트 전략

#### 7. LLM 기초
- Transformer: 모든 현대 LLM(파운데이션 모델)의 기반
- 토큰화
- 양자화(4bit, Q4), Apple MLX 4bit 네이티브 양자화 | GGUF: llama.cpp 전용 포맷


---

- 왜 MLX인가?

| 프레임워크 | Apple Silicon 지원 | 성능 |
|-----------|-------------------|------|
| PyTorch (MPS) | 부분 지원, 일부 연산 CPU 폴백 | 보통 |
| **MLX** | **네이티브 최적화** | **최고** |
| llama.cpp (Metal) | Metal GPU 가속 | 우수 |
| ONNX Runtime | CoreML 변환 필요 | 보통 |
MLX는 Apple이 만든 프레임워크로 Apple Silicon에 최적화되어 있습니다.

- mlx-lm이란?

MLX 위에 구축된 LLM 전용 라이브러리로, 다음 기능을 제공합니다:
- 모델 로딩 (HuggingFace 모델 자동 변환)
- 텍스트 생성 (generate, stream_generate)
- LoRA/QLoRA 파인튜닝
- 모델 양자화
- 어댑터 병합 (fuse)



