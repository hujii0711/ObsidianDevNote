
> claude --dangerously-skip-permissions
 ▶︎ .claude/settings.json 설정 내 deny 항목도 무시된다.


> JSON 출력 + 최대 턴 수 제한
```
claude -p "보안 취약점 검토해줘" \ --output-format json \ --max-turns 5 \ --permission-mode bypassPermissions
```

> docs와 CLAUDE.md가 젤 중요하다고 함(실밸개발자) -> lazy loading으로 다른 .md 참조할 수 있게 해야 함

> compact 40%로 설정하기(~/.claude/settings.json: CLAUDE_AUTOCOMPACT_PCT_OVERRIDE = "40")

> 프롬프트: 클로드 코드에 입력하는 지시나 문장을 의미한다.

>클로드 코드는 실행된 작업 디렉터리 단위로 세션을 기록하며, ~/.claude/projects 디렉터리에 저장한다.

>거버넌스: 어떤 조직이나 프로젝트에서 **"누가, 어떤 권한을 가지고, 어떻게 의사를 결정하고 책임질 것인가에 대한 규칙과 시스템"**을 이야기하고 싶을 때 '거버넌스'라는 단어를 사용하시면 됩니다.

>/plugin marketplace add revfactory/harness
  /plugin install harness@harness-marketplace
  /reload-plugins
